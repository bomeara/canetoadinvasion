---
title: "Cane Toad Invasion"
---

Cane toads invade

```{r, echo=FALSE, cache=FALSE}
source("R/packages.R")
source("R/functions.R")
```

```{r, cache = TRUE, echo=FALSE}
  locations <- GetCaneToadLocations()
```

```{r, cache = TRUE, echo=FALSE}
aus <- ggplot2::map_data("world", "Australia")
my_map <- ggplot() + geom_polygon(data = aus, aes(x=long, y = lat, group = group)) + coord_fixed(1.3) + geom_point(data=locations, aes(x=locations$decimalLongitude, y=locations$decimalLatitude), col="red")
print(my_map)
```
